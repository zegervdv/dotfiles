# Only be case sensitive if a case is present
--smart-case

# include .hgignore in ignores
--ignore-file=.hgignore

# Limit output
--max-columns=150
--max-columns-preview

--type-add
hdl:*.{sv,vhd,v}
--type-add
rtl:*.rtl.{sv,vhd}
